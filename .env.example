# Local demo starts without editing these. Set independent secrets for any shared deployment.
POSTGRES_PASSWORD=synthetic-demo-local-only
ENCRYPTION_KEY=
AUTH_SECRET=
# Hosted browser companion only: trusted public origin set through Sites runtime configuration.
SITE_ORIGIN=
# Optional custom PostgreSQL connection outside Compose:
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/idshield
# IDSHIELD_DATA_DIR=./data
# IDSHIELD_ALLOW_TUNNEL=1
# DEMO_OFFICER_PASSWORD=officer-demo
# DEMO_SUPERVISOR_PASSWORD=supervisor-demo
# DEMO_ADMIN_PASSWORD=admin-demo
